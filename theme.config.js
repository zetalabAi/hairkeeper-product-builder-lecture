/** @type {const} */
const themeColors = {
  // Primary: 핑크-퍼플 브랜드 컬러
  primary: { light: '#A855F7', dark: '#A855F7' },
  // Secondary: 핑크 악센트
  secondary: { light: '#EC4899', dark: '#EC4899' },
  // Background: 순수 검정 대신 미세한 색조
  background: { light: '#FAFAFA', dark: '#0A0A0B' },
  // Surface: 명도 차이로 레이어 구분
  surface: { light: '#FFFFFF', dark: '#1A1A1D' },
  // Foreground: 명확한 텍스트 대비
  foreground: { light: '#1A1A1A', dark: '#F5F5F5' },
  // Muted: 보조 텍스트
  muted: { light: '#6B6B6B', dark: '#A0A0A0' },
  // Border: subtle 구분선
  border: { light: '#E8E8E8', dark: '#2A2A2D' },
  // Success: 저채도 초록
  success: { light: '#4CAF50', dark: '#66BB6A' },
  // Warning: 저채도 주황
  warning: { light: '#FF9800', dark: '#FFA726' },
  // Error: 저채도 빨강
  error: { light: '#F44336', dark: '#EF5350' },
};

module.exports = { themeColors };
